/**
 * Sponsored ads. Served by the Cloudflare Worker in /worker/worker.js at the
 * repo root (same file also serves the default-lists manifest, routed by
 * query param — see /worker/README.md); this service fetches it with a
 * local cache and a bundled snapshot as last-resort fallback.
 */

export { createRegistryAdsService } from "./registry-ads";
