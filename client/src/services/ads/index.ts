/**
 * Sponsored ads. The registry lives in /ads at the repo root and is published
 * to Cloudflare Pages; this service fetches it with a local cache and a
 * bundled snapshot as last-resort fallback.
 */

export { createRegistryAdsService } from "./registry-ads";
